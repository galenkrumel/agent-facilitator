/**
 * Seeds the demonstration scenario — a normal decision already part-way
 * through Discuss — and prints its participant links.
 *
 * Implemented in M7. It is a no-op now so that `npm run setup` has a single
 * unconditional path from M0 onward. Seeding must stay idempotent: running it
 * repeatedly must not create duplicate decisions.
 */
console.log("  (no seed scenario yet — implemented in M7)");
